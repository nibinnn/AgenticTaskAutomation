# Docker command to start the app
docker compose up --build


### TO DO ITEMS

1. API KEY generate and test
2. Test the complicated use cases like multi-directional workflow
3. UI dashboard using steamlit  - Done with react js
4. User authentiation implementation
    - System prompts must be in accordance with user roles
    - user token management
    - user login feature
5. 


/*-------------- Not revelant or old notes ------------*/

# Docker command to start the app
docker compose --profile api up --build

# without build
docker compose --profile api up

# Other options to start
docker compose --profile api up --build
docker compose --profile dev up --build
docker compose up --build api


# If you remove profiles in docker-compose.yml, then you can start using 
docker compose up --build

-----------------------------------------------------------------------------------